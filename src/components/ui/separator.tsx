import { component$ } from "@builder.io/qwik";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { ComboboxControl } from "@qwik-ui/headless";

export const MySeparator = component$(() => {
  return <ComboboxControl></ComboboxControl>;
});
